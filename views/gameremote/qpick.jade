extends ../layoutmobile

block append head
  script(src='/javascripts/gameremote/controller.js')
  link(rel='stylesheet', href='/stylesheets/gameremote.css')

block title 
  title Pick a Question
  
block main
      #quespick(ng-controller="remote-cont", ng-init="gameid= #{ gameid }")
        h1 Pick a Question
        input#in(type='text', ng-model='numin')
        button#pick(ng-click="sendnum()") Send
